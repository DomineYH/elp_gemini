-- ===============================================================
-- Google API Document ID 저장을 위한 스키마 확장
-- 생성일: 2025-11-21
-- 목적: FileSearchStore 문서 삭제를 위한 document_id 추가
-- ===============================================================

-- ---------------------------------------------------------------
-- 1. criteria_documents 테이블에 document_id 컬럼 추가
-- ---------------------------------------------------------------
-- Google File Search Store에 업로드된 문서의 고유 ID 저장
-- 형식: "corpora/{corpus_id}/documents/{document_id}"
-- 용도: 삭제 시 문서→스토어 순차 삭제 지원

ALTER TABLE criteria_documents
  ADD COLUMN document_id TEXT NULL;

-- ---------------------------------------------------------------
-- 2. 인덱스 추가 (조회 성능 최적화)
-- ---------------------------------------------------------------
-- document_id 기반 조회 및 삭제 작업 성능 향상
CREATE INDEX IF NOT EXISTS idx_criteria_document_id
  ON criteria_documents(document_id);

-- ===============================================================
-- Migration Complete
-- ===============================================================
