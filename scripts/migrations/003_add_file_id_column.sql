-- ===============================================================
-- Google File ID 저장을 위한 스키마 확장
-- 생성일: 2025-11-21
-- 목적: 독립적인 파일 객체 ID 저장으로 개별 파일 삭제 지원
-- ===============================================================

-- ---------------------------------------------------------------
-- 1. criteria_documents 테이블에 file_id 컬럼 추가
-- ---------------------------------------------------------------
-- Google File Upload API로 업로드된 파일의 고유 ID 저장
-- 형식: "files/{file_id}"
-- 용도: 독립적인 파일 삭제 지원 (스토어 종속성 제거)

ALTER TABLE criteria_documents
  ADD COLUMN file_id TEXT NULL;

-- ---------------------------------------------------------------
-- 2. 인덱스 추가 (조회 성능 최적화)
-- ---------------------------------------------------------------
-- file_id 기반 조회 및 삭제 작업 성능 향상
CREATE INDEX IF NOT EXISTS idx_criteria_file_id
  ON criteria_documents(file_id);

-- ===============================================================
-- Migration Complete
-- ===============================================================
